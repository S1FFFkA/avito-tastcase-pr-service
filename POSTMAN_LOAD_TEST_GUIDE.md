# Пошаговая инструкция: Нагрузочное тестирование в Postman

## 📋 Шаг 1: Создание коллекции

1. Откройте Postman
2. Нажмите **"New"** → **"Collection"**
3. Назовите коллекцию: `AVITO PR System Load Test`
4. Нажмите **"Create"**

---

## 📝 Шаг 2: Добавление запросов в коллекцию

### 2.1. Создание команды (Create Team)

1. В коллекции нажмите **"Add request"** или **"..."** → **"Add request"**
2. Название: `Create Team`
3. Метод: **POST**
4. URL: `http://localhost:8080/team/add`
5. Вкладка **Headers**:
   - `Content-Type`: `application/json`
   - `Authorization`: `Bearer test-token` (если требуется)
6. Вкладка **Body** → выберите **raw** → **JSON**:
```json
{
  "team_name": "load-test-team-{{$randomInt}}",
  "members": [
    {
      "user_id": "user-{{$randomInt}}-1",
      "username": "User 1",
      "is_active": true
    },
    {
      "user_id": "user-{{$randomInt}}-2",
      "username": "User 2",
      "is_active": true
    },
    {
      "user_id": "user-{{$randomInt}}-3",
      "username": "User 3",
      "is_active": true
    },
    {
      "user_id": "user-{{$randomInt}}-4",
      "username": "User 4",
      "is_active": true
    },
    {
      "user_id": "user-{{$randomInt}}-5",
      "username": "User 5",
      "is_active": true
    }
  ]
}
```

### 2.2. Получение команды (Get Team)

1. Добавьте новый запрос: `Get Team`
2. Метод: **GET**
3. URL: `http://localhost:8080/team/get?team_name=load-test-team-{{$randomInt}}`
4. Headers: `Authorization: Bearer test-token`

### 2.3. Создание Pull Request (Create PR)

1. Добавьте новый запрос: `Create Pull Request`
2. Метод: **POST**
3. URL: `http://localhost:8080/pullRequest/create`
4. Headers:
   - `Content-Type`: `application/json`
   - `Authorization`: `Bearer test-token`
5. Body (raw JSON):
```json
{
  "pull_request_id": "pr-load-{{$randomInt}}",
  "pull_request_name": "Load Test PR {{$randomInt}}",
  "author_id": "user-{{$randomInt}}-1"
}
```

### 2.4. Переназначение ревьювера (Reassign Reviewer)

1. Добавьте новый запрос: `Reassign Reviewer`
2. Метод: **POST**
3. URL: `http://localhost:8080/pullRequest/reassign`
4. Headers:
   - `Content-Type`: `application/json`
   - `Authorization`: `Bearer test-token`
5. Body (raw JSON):
```json
{
  "pull_request_id": "pr-load-{{$randomInt}}",
  "old_user_id": "user-{{$randomInt}}-2"
}
```

### 2.5. Слияние PR (Merge PR)

1. Добавьте новый запрос: `Merge Pull Request`
2. Метод: **POST**
3. URL: `http://localhost:8080/pullRequest/merge`
4. Headers:
   - `Content-Type`: `application/json`
   - `Authorization`: `Bearer test-token`
5. Body (raw JSON):
```json
{
  "pull_request_id": "pr-load-{{$randomInt}}"
}
```

### 2.6. Получение ревью пользователя (Get User Reviews)

1. Добавьте новый запрос: `Get User Reviews`
2. Метод: **GET**
3. URL: `http://localhost:8080/users/getReview?user_id=user-{{$randomInt}}-1`
4. Headers: `Authorization: Bearer test-token`

---

## 🔧 Шаг 3: Настройка переменных коллекции

1. Откройте коллекцию → вкладка **"Variables"**
2. Добавьте переменные:
   - `base_url`: `http://localhost:8080`
   - `auth_token`: `Bearer test-token`
3. В запросах используйте: `{{base_url}}/team/add`

---

## 🎯 Шаг 4: Настройка Pre-request Script (опционально)

Для динамической генерации данных добавьте в коллекцию **Pre-request Script**:

```javascript
// Генерируем уникальный ID для каждого запроса
pm.collectionVariables.set("run_id", Date.now());
pm.collectionVariables.set("team_index", Math.floor(Math.random() * 5) + 1);
pm.collectionVariables.set("user_index", Math.floor(Math.random() * 5) + 1);
pm.collectionVariables.set("pr_index", Math.floor(Math.random() * 1000) + 1);
```

Используйте в запросах: `{{run_id}}`, `{{team_index}}`, `{{user_index}}`, `{{pr_index}}`

---

## 🚀 Шаг 5: Запуск нагрузочного тестирования (Collection Runner)

### 5.1. Открытие Runner

1. Нажмите на коллекцию → **"..."** (три точки)
2. Выберите **"Run collection"**
   ИЛИ
3. Нажмите **"Runner"** в верхнем меню
4. Перетащите коллекцию в Runner

### 5.2. Настройка параметров

В правой панели Runner настройте:

#### **Iterations (Итерации)**
- **Количество**: `100` (или больше для нагрузки)
- Это количество раз, которое каждый запрос будет выполнен

#### **Delay (Задержка)**
- **Delay between requests**: `200` миллисекунд
- Это задержка между запросами в одной итерации

#### **Data (Данные)**
- Можно загрузить CSV файл с тестовыми данными (опционально)

#### **Run Order (Порядок выполнения)**
- Выберите порядок запросов или оставьте по умолчанию

### 5.3. Выбор запросов для запуска

1. В списке запросов отметьте галочками те, которые хотите запустить:
   - ✅ Create Team
   - ✅ Get Team
   - ✅ Create Pull Request
   - ✅ Reassign Reviewer
   - ✅ Merge Pull Request
   - ✅ Get User Reviews

2. Можно изменить порядок, перетаскивая запросы

### 5.4. Запуск теста

1. Нажмите кнопку **"Run AVITO PR System Load Test"** (синяя кнопка внизу)
2. Наблюдайте за выполнением в реальном времени
3. После завершения увидите результаты

---

## 📊 Шаг 6: Анализ результатов

После завершения Runner покажет:

### **Summary (Сводка)**
- Общее количество запросов
- Успешные запросы (зеленые)
- Неудачные запросы (красные)
- Среднее время ответа
- Минимальное/максимальное время ответа

### **Request Details (Детали запросов)**
- Для каждого запроса:
  - Количество успешных/неудачных запросов
  - Среднее время ответа
  - Статус коды ответов

### **Export Results (Экспорт результатов)**
- Нажмите **"Export Results"** для сохранения в JSON/CSV

---

## 🎛️ Шаг 7: Настройка для разных сценариев нагрузки

### Сценарий 1: Низкая нагрузка (RPS = 5)
- **Iterations**: `50`
- **Delay**: `200ms`
- **Запросы**: Все

### Сценарий 2: Средняя нагрузка (RPS = 10-20)
- **Iterations**: `100`
- **Delay**: `100ms`
- **Запросы**: Все

### Сценарий 3: Высокая нагрузка (RPS = 50+)
- **Iterations**: `500`
- **Delay**: `50ms`
- **Запросы**: Все

### Сценарий 4: Пиковая нагрузка (RPS = 100+)
- **Iterations**: `1000`
- **Delay**: `10ms`
- **Запросы**: Все

---

## 📈 Шаг 8: Мониторинг метрик

Во время нагрузочного тестирования проверяйте метрики:

1. Откройте в браузере: `http://localhost:8080/metrics`
2. Ищите метрики:
   - `reviewer_load_distribution_bucket` - распределение нагрузки
   - `http_request_duration_seconds` - время ответа
   - `http_requests_total` - общее количество запросов

---

## ⚠️ Важные замечания

1. **Уникальные ID**: Используйте `{{$randomInt}}` или `{{run_id}}` для избежания конфликтов
2. **Порядок запросов**: Сначала создайте команды, потом PR
3. **Очистка данных**: Перед каждым тестом можно очистить БД или использовать уникальные ID
4. **Параллельность**: Postman Runner выполняет запросы последовательно. Для параллельных запросов используйте Newman CLI или несколько экземпляров Runner

---

## 🔄 Альтернатива: Newman CLI (для автоматизации)

Если нужно запускать из командной строки:

```bash
# Установка Newman
npm install -g newman

# Экспорт коллекции из Postman
# File → Export → Collection v2.1

# Запуск с параметрами
newman run collection.json \
  --iterations 100 \
  --delay-request 200 \
  --reporters cli,json \
  --reporter-json-export results.json
```

---

## 📝 Примеры JSON для запросов

### Create Team (с уникальными ID)
```json
{
  "team_name": "load-test-team-{{$timestamp}}",
  "members": [
    {"user_id": "user-{{$timestamp}}-1", "username": "User 1", "is_active": true},
    {"user_id": "user-{{$timestamp}}-2", "username": "User 2", "is_active": true},
    {"user_id": "user-{{$timestamp}}-3", "username": "User 3", "is_active": true},
    {"user_id": "user-{{$timestamp}}-4", "username": "User 4", "is_active": true},
    {"user_id": "user-{{$timestamp}}-5", "username": "User 5", "is_active": true}
  ]
}
```

### Create Pull Request
```json
{
  "pull_request_id": "pr-load-{{$timestamp}}",
  "pull_request_name": "Load Test PR {{$timestamp}}",
  "author_id": "user-{{$timestamp}}-1"
}
```

---

## ✅ Чек-лист перед запуском

- [ ] Сервер запущен на `http://localhost:8080`
- [ ] База данных доступна
- [ ] Все запросы добавлены в коллекцию
- [ ] Переменные настроены
- [ ] Уникальные ID используются ({{$randomInt}} или {{$timestamp}})
- [ ] Headers настроены (Authorization, Content-Type)
- [ ] Runner настроен (Iterations, Delay)
- [ ] Метрики доступны на `/metrics`

---

**Удачи с нагрузочным тестированием! 🚀**

