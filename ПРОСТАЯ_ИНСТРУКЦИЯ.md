# 🎯 ПРОСТАЯ ИНСТРУКЦИЯ: Где что настраивать

## ❗ ВАЖНО: RPS = 5 - это НЕ количество пользователей!

**RPS** = количество **запросов в секунду** (скорость отправки запросов)

---

## 📍 ШАГ 1: Откройте Runner

1. В Postman найдите коллекцию **"AVITO PR System Load Test"**
2. **Кликните один раз** на коллекцию (выделится)
3. Справа увидите кнопку **"Run"** → нажмите её

**ИЛИ**

1. Нажмите **три точки** (...) справа от коллекции
2. Выберите **"Run collection"**

---

## 📍 ШАГ 2: Где настраивать (ПРАВАЯ ПАНЕЛЬ)

После открытия Runner вы увидите **две колонки**:

### Левая колонка = список запросов
### Правая колонка = НАСТРОЙКИ ⚙️

**Смотрите в ПРАВУЮ колонку!**

Там будет:

```
┌─────────────────────────────┐
│ Run configuration           │
├─────────────────────────────┤
│ Iterations                  │
│ [20]                        │ ← ВОТ СЮДА!
│                             │
│ Delay                       │
│ [200] ms                    │ ← И СЮДА!
│                             │
│ Delay between iterations    │
│ [0] ms                      │
└─────────────────────────────┘
```

### Что вводить:

1. **Iterations**: напишите `20`
2. **Delay**: напишите `200` (уже должно быть "ms")
3. **Delay between iterations**: напишите `0`

---

## 📍 ШАГ 3: Выбор запросов (ЛЕВАЯ ПАНЕЛЬ)

В **левой колонке** отметьте галочками ✅ все 6 запросов:

- ☑ Create Team
- ☑ Get Team
- ☑ Create Pull Request
- ☑ Reassign Reviewer
- ☑ Merge Pull Request
- ☑ Get User Reviews

---

## 🚨 ПРОБЛЕМА: 409 ошибки

### Что это значит:
- `TEAM_EXISTS` = команда уже существует
- `PR_EXISTS` = PR уже существует

### ✅ РЕШЕНИЕ 1: Очистить базу данных

**Выполните SQL скрипт** `clear_db_for_test.sql` в вашей БД:

```sql
TRUNCATE TABLE reviewers CASCADE;
TRUNCATE TABLE pull_requests CASCADE;
TRUNCATE TABLE users CASCADE;
TRUNCATE TABLE teams CASCADE;
```

**Или через psql:**
```bash
psql -U your_user -d your_database -f clear_db_for_test.sql
```

### ✅ РЕШЕНИЕ 2: Игнорировать 409 ошибки

Если вы запускаете тест повторно, 409 ошибки **нормальны** - это значит, что данные уже есть.

**Просто игнорируйте их** или очистите БД перед тестом.

---

## 📊 ШАГ 4: Как посмотреть результаты

### После завершения теста:

1. **Прокрутите вниз** в окне Runner
2. Увидите секцию **"Summary"**:
   - Total requests
   - Failed requests
   - Average response time

3. **Для каждого запроса** увидите:
   - Количество успешных/неудачных
   - Среднее время ответа
   - График (если есть)

---

## 📤 ШАГ 5: Как отправить отчет

### Вариант 1: Скриншот
1. Сделайте скриншот экрана с результатами
2. Отправьте мне

### Вариант 2: Экспорт результатов
1. В Runner нажмите кнопку **"Export Results"** (внизу справа)
2. Выберите формат **JSON**
3. Сохраните файл
4. Отправьте мне файл или скопируйте содержимое

### Вариант 3: Скопировать текст
1. В Runner выделите текст результатов
2. Скопируйте (Ctrl+C)
3. Вставьте сюда

---

## 🎯 КРАТКАЯ ПАМЯТКА:

```
1. Откройте Runner: Коллекция → "Run"
2. ПРАВАЯ ПАНЕЛЬ: Iterations = 20, Delay = 200ms
3. ЛЕВАЯ ПАНЕЛЬ: Отметьте все 6 запросов ✅
4. Нажмите "Run"
5. Дождитесь завершения
6. Посмотрите результаты внизу
7. Экспортируйте или сделайте скриншот
```

---

## 💡 Если не понятно:

**Опишите, что вы видите на экране**, и я помогу!

Или **пришлите скриншот** экрана Runner, и я покажу точно, куда нажимать.

---

## 🔧 Быстрая проверка настроек:

После настройки должно быть:
- ✅ Iterations: **20**
- ✅ Delay: **200** ms
- ✅ Отмечено **6 запросов**
- ✅ БД очищена (или игнорируем 409 ошибки)

**Готово! Запускайте! 🚀**

